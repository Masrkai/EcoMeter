# Task 5 Modulation

the code has a requirement of calling satelite imagery and downloading it every time when it works
find a workaround that save that response if used once and use that saved response if it's present instead of re-fetching it every time